import { Page, Panel } from './CurrencyPanelStyle';

export const CurrencyPanel = () => (
  <Page>
    <Panel>
      <div>UsersTable</div>
      <div>GraphCurrency</div>
    </Panel>
  </Page>

  // <Provider store={store}>

  // </Provider>
);
