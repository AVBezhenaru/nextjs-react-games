import { DifficultyLevel } from '../types/difficulty';

export const difficultyLevelPlug: DifficultyLevel = {
  lifetime: 0,
  lives: 0,
  targetSize: 0,
  defaultSpeed: 0,
  speedIncrementPerSecond: 0,
  maxTargetCount: 0,
};
